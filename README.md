# spaceship-operator

C++20 spaceship operator (`<=>`) exploration and utilities.

## Build

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

## Requirements

- C++20 compatible compiler (MSVC 19.28+, GCC 10+, Clang 10+)
- CMake 3.20+
